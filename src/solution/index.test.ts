import app from '../app';
import request from 'supertest';
import {expect} from 'chai';

describe('GET /solution', () => {
    it('should return ', () => {
        return request(app)
        // .get('/solution')
        // .expect()
    });
})